name: Deploy to staging

# on: [ workflow_dispatch ]
on:
  push:
    branches: [ action/staging ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
  
    steps:
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v1
      env:
        TEST: ${{ secrets.TEST }}
